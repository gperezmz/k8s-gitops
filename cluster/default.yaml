---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: default
  namespace: flux-system
spec:
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  dependsOn:
    - name: system
    - name: monitoring-system
  interval: 10m0s
  path: ./default
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client
